<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/header.ejs') %>

</head>
<body>
    <h1>What Are We Writing Today</h1>
    <%- include('./partials/nav.ejs') %>
    <div>
        <h2>All Blogs</h2>
        <% if (blogs.length > 0){ %>
            <% blogs.forEach( blog => { %>
                <h3 class="title"><%= blog.title %></h3>
                <p class="snippet"><%= blog.snippet %></p>
        <% }) %>
        <% } else { %>
        <p>There no blogs to display...</p>
        <% } %>
    </div> 
</body>
</html>